import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFFFFF4E6);
  static const black = Color(0xFF111111);
  static const white = Colors.white;

  static const primary = Color(0xFFFF6A2A);
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);

  static const cardYellow = Color(0xFFFFC83D);
  static const cardGreen = Color(0xFF2F9275);
  static const cardBlue = Color(0xFF84C5DD);

  static const darkBackground = Color(0xFF171615);
  static const darkSurface = Color(0xFF242220);
  static const darkElevated = Color(0xFF2E2C29);
  static const darkHighlight = Color(0xFF3A3733);

  // Ink scale
  static const inkPrimary = Color(0xFFF5EDE0);
  static const inkSecondary = Color(0xFFB8AFA5);
  static const inkMuted = Color(0xFF6D6860);
  static const inkBorder = Color(0xFF3A3733);

  // Brand (bumped for contrast on dark)
  static const primaryDark = Color(0xFFFF7A3D);
  static const primarySubtle = Color(0xFF3A1A0A);
  static const successDark = Color(0xFF5DC060);
  static const errorDark = Color(0xFFF05252);

  // Card accents (re-tuned for dark bg)
  static const cardYellowDark = Color(0xFFCC9F28);
  static const cardGreenDark = Color(0xFF3AAD88);
  static const cardBlueDark = Color(0xFF6BAFC8);
}
